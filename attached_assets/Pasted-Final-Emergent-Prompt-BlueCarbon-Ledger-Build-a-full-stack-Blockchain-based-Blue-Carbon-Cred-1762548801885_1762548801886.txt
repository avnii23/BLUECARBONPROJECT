Final Emergent Prompt — “BlueCarbon Ledger” 🌊

Build a full-stack Blockchain-based Blue Carbon Credit Registry web application (prototype) called BlueCarbon Ledger, with a sleek, futuristic, and minimalist ocean-themed UI/UX.

Roles and Authentication:

Admin: username admin, password admin123

Verifier: username verifier, password verifier123

User/Company: dummy signups with email and password (no email verification required).



---

🌿 Core Features & Blockchain Logic

When a verifier approves a project, create a transaction with:

txId = SHA-256(project data + timestamp + random salt)

from = system/issuer

to = userId

credits = CO₂ absorbed (in tons)

projectId, timestamp, proofHash (e.g., uploaded image/document hash)


Group verified transactions into blocks that include:

index, timestamp, transactions[], merkleRoot, previousHash, nonce or validatorSignature, and blockHash

Use SHA-256 for all hashes and display them in the UI (abbreviate in lists, expandable for full view).


Compute Merkle Root from transaction hashes.

Store each block immutably (JSON/local DB simulation).

Optional simple Proof-of-Authority (signed by verifier/admin).

Show block internals in a Public Ledger / Explorer View — users can search by txId, blockHash, or project name.

Add hash verification tool so users can recompute a hash client-side and confirm “Verified ✅” or “Tampered ❌.”



---

🧭 Dashboards

🧍‍♂ User Dashboard

After login, show a personal dashboard with:

My Projects: table listing project name, status (Pending / Verified / Rejected), CO₂ captured, and proof files.

My Credits: total verified BlueCarbon Credits (sum of verified projects).

My Transactions: list of txIds with short hashes and approval timestamps.

Option to click a project → view its blockchain record (txId, blockHash, verifier signature).

A visual counter or progress circle showing total credits accumulated.

Option to download a Project Certificate / Receipt (PDF or JSON) containing project details, txId, and blockHash.



🧑‍💼 Admin Dashboard

Comprehensive view of all projects submitted by all users.

Filter by status: Pending / Verified / Rejected.

Ability to assign projects to verifiers.

See blockchain info (blockHash, merkleRoot, txCount).

Manage users & verifiers (add/remove).

View entire blockchain structure and export it (JSON/CSV).

See global stats: total projects, total verified credits, total CO₂ captured.



🧾 Verifier Dashboard

List of pending projects waiting for verification.

Can open project details → view uploaded proof → Approve or Reject.

On approval → automatically generates a transaction + adds to next block in chain.

Can view own signed/approved transactions and blocks.



---

⚙ Blockchain Display

Blocks shown as collapsible cards with:

Block #, Timestamp, MerkleRoot, PreviousHash, BlockHash

Inside: transaction summaries (user, project, credits, txId)


Public Explorer Page:

Search by blockHash / txId / projectId

Show block verification animation

Copy-to-clipboard for hashes

Tooltip explaining txId, MerkleRoot, BlockHash




---

🌊 UI/UX & Theming

Ocean-themed palette: gradients of aqua, turquoise, and deep navy.

Minimal futuristic UI: soft shadows, clean typography, rounded edges.

Floating bubbles, coral accents, subtle wave animations on the login & dashboard backgrounds.

Responsive for mobile and desktop.

Accessibility: high-contrast text, copy buttons for hashes, tooltips for technical terms.

Add a dark mode toggle (aqua light vs deep-sea dark).



---

🧠 Tech Stack (suggested)

Frontend: React / Emergent UI Components

Backend: Node.js + Express

Blockchain Layer: JavaScript-based simulated chain with SHA-256 hashing (crypto.subtle or node:crypto)

Database: JSON, SQLite, or MongoDB for prototype



---

🧩 Extra polish

Preload a few sample users, verifiers, and demo projects on first launch.

Add a small “How it works” walkthrough modal explaining blockchain transparency for non-technical users.

Add a real-time carbon counter on the homepage showing “Total CO₂ Captured by All Projects.”

Display demo login credentials (admin/verifier/sample user) on the login page for easy testing.

Allow exporting entire blockchain data (with hashes) as proof-of-transparency.


Goal:
Create a fully functional prototype that mimics a real-world Blue Carbon Credit Registry, demonstrating blockchain transparency, verifier authority, and user dashboards — all within an ocean-inspired, futuristic interface.